class SystemPropertyPipeLength extends FlowModelTemplate {
    constructor(parent, label, user, unitsValue, method,)
    {
        super(parent, label, user, unitsValue, method, "systemPropertyPipeLength");

        this.units.quantity = "length";

        this.methods = {};
    }
}