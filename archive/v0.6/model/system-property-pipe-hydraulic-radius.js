class SystemPropertyPipeHydraulicRadius extends FlowModelTemplate {
    constructor(parent, label, user, unitsValue, method,)
    {
        super(parent, label, user, unitsValue, method, "systemPropertyPipeHydraulicRadius");

        this.units.quantity = "length";

        this.methods = {};
    }
}